var config = {
  server : 'http://foo.com',
  interval : 5 * 1000,
  directory : './remote',
  force: true,
  owner : 'nginx',
  group : 'nginx',
  auth : {
    username : '',
    password : ''
  }
};

module.exports = config;
